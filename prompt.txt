你是一名资深 Java/Spring/Maven 开发者，请阅读当前仓库代码与配置，为本项目生成 README.md，供后续大模型写代码时作为
  上下文指引。输出仅包含 README 内容本身。要求只输出下面的内容：

  1) 顶部预留一段「我的总结」空白块，用 Markdown 标题和占位文本标出。
  3) 关键类与核心代码：
     - 按模块或包名列出所有关键类，每个类用一句话解释逻辑/意图。
     - 需要把每个类的核心代码片段写出来。
  4) 调用/依赖关系概览：
     - 文字或简短列表描述初始化与调用链（启动 -> 配置装配 -> 传输/控制层 -> 工具/服务等）。
     - 工具/服务的调用链（请求入口 -> 传输/路由 -> Provider/Service 方法）。
注意：用中文输出，只输出上面要求的内容，其他内容一律不输出！！！